# Portfólio Fotógrafa - HTML/CSS/JS

Abra `index.html` no navegador ou use uma extensão Live Server no VSCode.
Coloque suas fotos em `assets/galeria/` e substitua `cliente1.jpg`, `cliente2.jpg`, `cliente3.jpg` pelas fotos dos clientes.
A logo já foi otimizada para `logo.webp` + `logo.png` e um `favicon.ico` foi gerado automaticamente.

## Ajustes rápidos
- Links das redes sociais: edite em `index.html` na seção Contato.
- Cores e fontes: personalize em `style.css` (variáveis CSS no topo).
- SEO e Open Graph: já configurados no `<head>` do `index.html`.

Bom trabalho! 📸